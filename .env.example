# App
# The port NestJS listens on. Defaults to 3000 if unset.
PORT=3000

# Database
# Replace credentials/host/db to match your local or cloud Postgres.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/ddd_prisma?schema=public

# Node environment
NODE_ENV=development

# Notes:
# 1) Copy this file to ".env" and adjust the values for your setup.
# 2) Prisma CLI and the app will read variables from .env automatically.
# 3) With docker-compose in this repo, the default DATABASE_URL works out of the box.
